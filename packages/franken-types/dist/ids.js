/**
 * Branded ID types for type-safe identifiers across modules.
 * All IDs are strings at runtime but distinct at compile-time.
 */
export function createProjectId(id) {
    return id;
}
export function createSessionId(id) {
    return id;
}
export function createTaskId(id) {
    return id;
}
export function createRequestId(id) {
    return id;
}
export function createSpanId(id) {
    return id;
}
export function createTraceId(id) {
    return id;
}
//# sourceMappingURL=ids.js.map