/**
 * Removes all files from the .build/ directory (logs, checkpoints, traces db).
 * Returns the number of files removed.
 */
export declare function cleanupBuild(buildDir: string): number;
//# sourceMappingURL=cleanup.d.ts.map